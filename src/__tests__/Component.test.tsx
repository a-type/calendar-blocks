import * as React from 'react';

describe('Calendar component', () => {
  // it('renders children', () => {
  //   const result = render(<Calendar data-testid="component"></Calendar>);
  //   expect(result.getByTestId('component')).toHaveTextContent('Hi');
  // });
});
