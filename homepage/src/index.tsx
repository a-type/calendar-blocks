import * as React from 'react';
import * as ReactDOM from 'react-dom';

const App = () => {
  return <main>There's nothing here yet</main>;
};

ReactDOM.render(<App />, document.getElementById('root'));
